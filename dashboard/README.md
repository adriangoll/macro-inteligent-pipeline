# Dashboard (Streamlit)

Aplicación de visualización (placeholders, sin lógica de negocio).

- `app.py` — punto de entrada y navegación.
- `pages/` — pantallas: Macro Argentina, Mercados globales, Correlaciones, AI Analyst.
- `components/` — componentes reutilizables.
- `assets/` — recursos estáticos.

Ejecución prevista (una vez implementado):

```bash
streamlit run dashboard/app.py
```
